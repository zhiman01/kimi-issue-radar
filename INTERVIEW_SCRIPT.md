# DevRel Content Intern 面试要点与话术参考（10 分钟版）

> ⚠️ **重要提示：这不是背诵稿**。
>
> 下面是观点清单和推荐话术，**请读完后合上文件，用你自己的话复述**。能复述出来的才是你的，背下来的都是 AI 的。
>
> DevRel 岗位的核心能力是"用自己的话把技术讲清楚"。面试官追问稿子外的细节时，背稿会立刻卡壳。
>
> 配套 demo：`/Users/wei/kimi-issue-radar/`
> 核心文件：`report.md`、`EVOLUTION.md`、`radar.py`

---

## 整体节奏（10 分钟）

| 段落 | 时间 | 核心目标 |
|---|---|---|
| 开场 | 1 min | 建立"你不是海投，是理解 DevRel" |
| 架构 | 1.5 min | 展示系统设计能力 |
| 迭代 | 2 min | 展示工程兜底和解决问题能力 |
| 洞察 | 2.5 min | 展示商业嗅觉和从数据提炼结论 |
| 战略意义 | 1.5 min | 展示"你能为公司做什么" |
| 岗位匹配 | 1 min | 回扣 JD 关键词 |
| 收尾 | 1 min | 邀请追问，控场 |

---

## 开场：为什么做这个项目（1 分钟）

**核心观点：**
- DevRel 不是被动回复工单，而是把社区噪音变成产品信号
- 大模型竞争已经从"模型有没有"变成"开发者能不能用起来"
- 这个 demo 验证：系统性读 GitHub Issues，能否比零散看帖更快找到文档/产品行动点

**推荐话术：**

> "我申请 DevRel Content Intern，是因为我相信开发者内容不是写教程，而是回答'开发者现在最卡在哪里'。我想验证一个假设：如果我们能系统性地读懂 GitHub Issues，能不能比零散看帖更快找到文档该写什么、产品该修什么？"

**动作：** 打开 `report.md` 第一页，指向执行摘要。

---

## 1. 项目架构：三层设计（1.5 分钟）

**核心观点：**
- 抓取层：GitHub API，过滤 PR，保留必要字段，body 截断
- 分析层：Kimi API 做受限分类，8 个类目 + severity + evidence
- 报告层：总量、分布、blocker 清单、核心洞察

**推荐话术：**

> "项目分三层。抓取层分页拉 GitHub Issues，注意这个接口会把 PR 混进来，所以必须用 `pull_request` 字段过滤。分析层每批 14 条送入 Kimi，做受限分类，不是让模型自由发挥。报告层输出 blocker 清单和核心洞察。"

**动作：** 打开 `radar.py`，翻到 `CATEGORY_DEFS` 和 `PROMPT_TEMPLATE`。

---

## 2. 迭代过程：真实 API 教会我的事（2 分钟）

**核心观点（挑 2-3 个讲）：**
- temperature 必须=1，Moonshot API 当前所有模型都锁定
- K3 对大批量长文本推理超时，降级到 `kimi-k2.7-code-highspeed`
- max_tokens 不足会导致空返回 / JSON 截断，最终稳定在 12000
- batch size 从 18 降到 14
- 432 条 issue 全部分析成功，0 个 batch 因代码失败

**推荐话术（模型选择，必讲）：**

> "我先试了 K3，但 14 条 × 800 字符的 batch 在 180 秒里经常回不来。分类任务对推理深度要求不高，对稳定性和速度要求高，所以我选了 `kimi-k2.7-code-highspeed`。这是任务匹配，不是 K3 不行。"

**动作：** 打开 `EVOLUTION.md`，翻调试记录。

---

## 3. 核心洞察：数据告诉我们什么（2.5 分钟）

**核心观点：**
- `model_behavior` 仅 7 条（1.6%），`agent_runtime` + `ide_integration` 占 41%
- 开发者不抱怨模型不够聪明，抱怨"装不上、连不上、跑到一半挂了"
- 88 个 blocker 中大量是"静默挂起"：不报错，直接卡死
- 付费用户额度体验差：无预警、无降级、无成果保全

**推荐话术（洞察 1）：**

> "最反直觉的发现是 model_behavior 只有 7 条，占 1.6%。而 agent_runtime 加 ide_integration 占了 41%。这说明开发者几乎不抱怨 Kimi 不够聪明，他们卡在最后一公里的工程体验。"

**推荐话术（洞察 2）：**

> "88 个 blocker 里我注意到一个模式：很多严重问题不是报错，而是静默卡死。报错还能排查，静默卡死只会导致卸载。"

**动作：** 打开 `report.md`，跳到「核心洞察」。

---

## 4. 战略意义：如果我是 DevRel 我会推什么（1.5 分钟）

**核心观点：**
- 文档重心从"教你怎么用"转向"教你怎么不翻车"
- 建立失败模式的显性反馈机制
- 把社区反馈监控常态化

**推荐话术：**

> "基于这些洞察，我会推动三件事。第一，文档不要只展示能力，要写 Windows 安装被拦截怎么办、遇到 429 怎么排查。第二，任何失败都必须向用户显性反馈，不能静默挂起。第三，把这个雷达 cron 化，每周输出新增 blocker 和文档缺口简报。"

---

## 5. 岗位匹配：为什么我能做好这个实习（1 分钟）

**核心观点：**
- 深入社区：抓取了 432 条真实 issue
- 开发者视角：自己调 Kimi API，踩过真实坑
- 自动化工具：Python + Kimi API 的反馈整理管道
- 文档优化：洞察直接指向文档该补什么
- AI Coding 生态：issue 覆盖 VSCode 插件、MCP、Agent 运行时

**推荐话术：**

> "这个岗位 JD 里的关键词——深入开发者社区、用 AI Agent 和自动化工具改进工作流、参与文档建设——这个项目全部覆盖了。而且我有理工科背景，能把内容工作和工程工作结合起来。"

---

## 6. 收尾与邀请提问（1 分钟）

**核心观点：**
- 表达入职后的第一个动作
- 邀请面试官追问具体部分

**推荐话术：**

> "如果我有幸加入，我想做的第一件事就是把这个雷达常态化：自动监控 GitHub 和 Discord 上的开发者反馈，每周输出一份新增 blocker 加文档缺口简报。这个项目的代码和报告都在我 GitHub 上，面试官对哪个部分最感兴趣？"

---

## 三个必须主动说明的风险点

### 风险 1：模型选择

**主动说：**

> "我先试了 K3，但批量长文本下超时严重。分类任务对推理深度要求不高、对稳定性和速度要求高，所以我选了 `kimi-k2.7-code-highspeed`。这是任务匹配，不是 K3 不行。"

### 风险 2：样本偏差

**主动说：**

> "432 条是近期活跃 issue 的采样，不是全量。kimi-code 实际约 764 条，我只取了最近 334 条；kimi-agent-sdk 98 条是全量。趋势和 blocker 模式够用，但精确占比会有偏差。"

### 风险 3：AI 代笔嫌疑

**不要照着这份文件背**。读完后用自己的话讲，讲得出来才是你的。

---

## 备用追问应答（读一遍，理解后用自己的话答）

### Q1：你这个项目最大的局限是什么？

**要点：**
- 类目体系是先验的，可能遗漏维度
- body 截断到 800，可能丢失关键上下文
- 432 条是采样，不是全量

### Q2：如果给你更多时间，你会加什么？

**要点：**
- 时间趋势分析
- 首次响应时长统计
- 自动生成"建议补充的文档条目"清单

### Q3：你为什么不用现成的 issue 分析工具？

**要点：**
- 现成工具能聚类，但难把归属方、severity、evidence 结构化
- 我希望产出能直接进文档 backlog 的结论

### Q4：你怎么保证模型分类不是瞎猜？

**要点：**
- 受限枚举，不能自由发挥
- 强制 evidence，可人工抽查
- 异常时标 other，不混入错误结论

### Q5：这个项目和你申请的 DevRel Content 有什么关系？

**要点：**
- Content Intern 要知道"写什么"
- 这个雷达把"我觉得开发者需要"变成"数据证明开发者需要"
