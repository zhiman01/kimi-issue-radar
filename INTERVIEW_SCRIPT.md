# DevRel Content Intern 面试演讲稿（10 分钟版）

> 配套 demo：`/Users/wei/kimi-issue-radar/`
> 核心文件：`report.md`、`EVOLUTION.md`、`radar.py`

---

## 开场：为什么做这个项目（1 分钟）

**【讲】**

我申请 DevRel Content Intern，是因为我认为开发者关系不是被动回复工单，而是主动把社区里的“噪音”变成产品的“信号”。

现在大模型公司的竞争已经从“模型能力有没有”转到“开发者能不能把模型用起来”。所以我想验证一个假设：**如果我们能系统性地读懂 GitHub Issues 里的开发者反馈，能不能比零散看帖更快找到文档该写什么、产品该修什么？**

为了验证这个假设，我花了大概一个晚上做了这个“Kimi 社区 Issue 洞察雷达”。它自动抓取了 MoonshotAI 两个核心仓库——`kimi-code` 和 `kimi-agent-sdk`——的全部 issue，共 **432 条**，然后用 Kimi API 做受限分类和归因，输出了一份 DevRel 洞察报告。

**【动作】**

打开 `report.md` 第一页，手指向总量和时间跨度表格。

**【关键词】**

深入开发者社区、收集一线产品反馈、社区洞察。

---

## 1. 项目架构：三层设计（1.5 分钟）

**【讲】**

整个项目分成三层，我故意保持简单，因为 MVP 的目标不是做一个复杂系统，而是验证“自动化洞察”这条路能不能跑通。

**第一层是抓取层**。我调的是 GitHub 的 `/issues` 接口。注意这个接口会把 Pull Request 也混在 issues 里返回，所以必须用 `pull_request` 字段是否存在来过滤。我只保留了分析必需的字段：number、title、body、state、labels、created_at、closed_at、comments、html_url。body 太长对聚类没增益，还吃 token，所以做了截断。

**第二层是分析层**，这是核心。我没有让模型自由发挥分类，而是设计了一套**受限分类体系**，共 8 个类目：

- install_env
- ide_integration
- auth_billing
- model_behavior
- agent_runtime
- docs_gap
- feature_request
- other

每个类目都对应一个归属方——文档、产品、模型。同时每条 issue 还要输出 severity：blocker、friction、nice_to_have，以及 one_line 和 evidence。

这里我加了一个非常硬的约束：**evidence 必须是 issue 原文的连续片段，不能改写、不能概括**。为什么？因为不带证据的分类就是模型拍脑袋，DevRel 拿这种结论去推产品是没有说服力的。

**第三层是报告层**。输出总量、open/closed 比例、类目分布、blocker 清单，以及 Top 3 洞察。

**【动作】**

打开 `radar.py`，快速翻到 `CATEGORY_DEFS` 和 `PROMPT_TEMPLATE`，展示分类体系和硬约束。

**【关键词】**

使用 AI Agent 和自动化工具改进反馈整理、社区洞察 workflows。

---

## 2. 迭代过程：真实 API 教会我的事（2 分钟）

**【讲】**

这部分可能是最有意思的。我一开始按 Spec 写的脚本，在 `--mock` 模式下跑得很顺，但一接真实 Kimi API 就连续翻车。我把每一步都记在了 `EVOLUTION.md` 里，这里讲三个最典型的坑。

**第一个坑是 temperature。**

我习惯按 OpenAI 的玩法设 `temperature=0.2`，结果 Kimi API 直接返回 400：`invalid temperature: only 1 is allowed for this model`。我试了 K3、K2.7、K2.6，全部锁定 `temperature=1`。这说明调用国产大模型 API 不能想当然地套 OpenAI 经验。

**第二个坑是 K3 大 batch 超时。**

Spec 说每批 15-20 条，我设了 18 条、body 1500 字符，结果 K3 在 180 秒超时窗口内根本回不来。我专门做了模型选型实验：K3 超时，K2.6 超时，`kimi-k2.7-code` 要 114 秒，只有 `kimi-k2.7-code-highspeed` 能在 24 秒左右稳定返回。所以最终降级到 highspeed 模型。

**第三个坑是 max_tokens 导致的空返回和 JSON 截断。**

我最初设了 4000，结果模型有时返回空 content，有时 JSON 输出到一半被截断，解析直接失败。后来逐步调到 8000、12000，同时把 batch 从 18 降到 14，才稳定跑完全部 432 条，31 个 batch，**0 次代码级失败**。

这段经历让我意识到：做 DevRel 自动化工具，**Prompt 设计只是 30%，剩下 70% 是工程兜底**——重试、超时、截断、空返回、JSON 容错。

**【动作】**

打开 `EVOLUTION.md`，翻到“迭代过程”部分，给面试官看真实的调试记录。

**【关键词】**

AI Coding、Agent、大模型 API 相关项目；独立解决问题的能力。

---

## 3. 核心洞察：数据告诉我们什么（2.5 分钟）

**【讲】**

讲完技术，回到业务。报告里最重要的发现，不是哪个类目数量最多，而是**哪些数字组合在一起揭示了结构性问题**。

### 洞察 1：模型能力不是瓶颈，接入与运行时才是

`model_behavior` 只有 **7 条**，占 **1.6%**，是全部类目里最少的。而 `agent_runtime` **108 条**、`ide_integration` **70 条**，两者合计 **178 条**，占 **41%**。

这说明什么？开发者几乎不抱怨 Kimi 模型不够聪明，他们抱怨的是“装不上、连不上、跑到一半挂了”。对一家以模型能力著称的公司，这是积极信号——模型已经不是短板，真正的护城河在最后一公里的工程体验。

### 洞察 2：一整类“静默挂起”问题正在侵蚀新用户信任

我仔细看 88 个 blocker 时发现一个模式：很多严重问题不是报错，而是**静默卡死**。比如：

- #2358：`kimi -p` 非交互模式挂起、零输出
- #2219：Windows 每次 prompt 永久卡死
- #1798：流式响应中途静默
- #1796：遇到 HTTP 429 直接吞掉错误和 Retry-After
- #1911：K3 max effort 下挂起 10 分钟以上

报错尚可排查，静默卡死只会让用户直接卸载。

### 洞察 3：付费用户的额度体验正在制造流失

有几条反馈让我印象很深：

- #2389：额度耗尽时静默杀掉所有 subagent，会话空转数小时
- #1482：5 小时 deep research 的 token 全部作废
- #158：刚充月订阅，用 4 次就被限流
- #133：年订阅用户遭遇持续 401

对 API First 公司，这不是小 bug，而是直接影响营收留存的设计问题。

### 附：K3 强制思考的 token 浪费

#1955“20k tokens to say hi”和 #2364“思考 30 分钟耗尽 token”指向 K3 的强制思考机制把简单任务也拖成长思考。我自己第一次调 K3 API 时也复现了类似现象——请求只想要两字回复，却因为思考没结束撞上 token 上限返回空结果。

**【动作】**

打开 `report.md` 跳到「## 4. 核心洞察」，逐条指给他看。

**【关键词】**

从开发者视角体验产品、发现使用过程中的问题与门槛、让复杂能力更容易被理解。

---

## 4. 战略意义：如果我是 DevRel 我会推什么（1.5 分钟）

**【讲】**

基于这些洞察，如果我是 DevRel，我会推动三件事。

**第一，文档重心从“教你怎么用”转向“教你怎么不翻车”。**

现在官方文档可能更多在展示能力，但开发者真正需要的是：Windows 安装遇到 Smart App Control 怎么办？遇到 429 怎么排查？额度耗尽前有没有预警？这些都是高杠杆内容。

**第二，建立失败模式的显性反馈机制。**

“静默挂起”不是一个 bug，是一类 bug。建议产品和工程团队加一个原则：任何 LLM 请求失败、任何工具调用失败、任何额度相关异常，都必须至少输出一行人类可读的信息，而不是让 TUI 凝固。

**第三，把社区反馈监控常态化。**

这次雷达是我手动跑一次脚本，但它完全可以 cron 化：每天/每周自动抓取新增 issue，生成趋势报告，定位哪些 blocker 是新增、哪些是 regression。这样 DevRel 就能从“救火”变成“预警”。

**【关键词】**

开发者文档建设与优化、技术内容、社区交流、AI Agent 自动化 workflows。

---

## 5. 岗位匹配：为什么我能做好这个实习（1 分钟）

**【讲】**

这个岗位 JD 里提到几个关键词：深入开发者社区、从开发者视角体验产品、用 AI Agent 和自动化工具改进工作流程、参与开发者文档建设、关注 AI Coding 生态。

我觉得这个项目正好覆盖了全部：

- **社区洞察**：我抓取了真实的 432 条开发者反馈，不是空想。
- **开发者视角**：我自己调了 Kimi API，踩了 temperature、max_tokens、超时这些真实开发者会踩的坑。
- **自动化工具**：整个雷达就是一套自动化反馈整理工具。
- **文档建设**：报告里的每一条洞察都直接对应“文档该补什么”。
- **AI Coding 生态**：我分析的 issue 本身就覆盖了 VSCode 插件、MCP、Agent 运行时这些最前沿的开发者工具链。

而且我有理工科背景，写代码、读日志、调 API 这些对我来说不是障碍，这让我能把内容工作和工程工作结合起来。

**【关键词】**

理工科背景、技术博客/开源项目/Demo/最佳实践。

---

## 6. 收尾与邀请提问（1 分钟）

**【讲】**

如果我有幸加入，我想做的第一件事就是把这次雷达的能力常态化：让它自动监控 GitHub 和 Discord 上的开发者反馈，每周输出一份“新增 blocker + 文档缺口”简报，直接同步给文档团队和产品团队。

这个项目的代码、报告、迭代记录都在我 GitHub 上，我也可以现场跑一遍给你看。

不知道面试官对哪个部分最感兴趣？是分类体系的设计、Prompt 的硬约束、还是某个具体洞察？

**【动作】**

合上电脑或保持 `report.md` 打开，留出提问空间。

---

## 10 分钟节奏控制表

| 段落 | 用时 | 核心目标 |
|---|---|---|
| 开场 | 1 min | 建立“你不是海投，是理解 DevRel” |
| 架构 | 1.5 min | 展示系统设计能力 |
| 迭代 | 2 min | 展示工程兜底和解决问题能力 |
| 洞察 | 2.5 min | 展示商业嗅觉和从数据提炼结论 |
| 战略意义 | 1.5 min | 展示“你能为公司做什么” |
| 岗位匹配 | 1 min | 回扣 JD 关键词 |
| 收尾 | 1 min | 邀请追问，控场 |

---

## 备用追问应答

### Q1：你这个项目最大的局限是什么？

**【答】**

两个局限。

一是类目体系是我先验设计的，可能漏掉了一些开发者真正关心的维度，比如“性能”或“多语言支持”，后续应该用无监督聚类辅助验证。

二是 body 截断到 800，虽然避免了日志噪音，但也可能丢失关键上下文。下一步我会做 ablation：比较 body=400/800/1500 对分类一致性的影响。

### Q2：如果给你更多时间，你会加什么？

**【答】**

三个方向：

1. **时间趋势**——看哪些类目在上升；
2. **首次响应时长**——衡量社区运营健康度；
3. **自动生成“建议补充的文档条目”清单**——把洞察直接转成文档 backlog。

### Q3：你为什么不用现成的 issue 分析工具？

**【答】**

现成的工具能聚类，但很难把分类体系、归属方、severity、evidence 这些 DevRel 关心的字段结构化。我希望产出的是能直接进文档 backlog 的结论，而不是一个词云。

### Q4：你怎么保证模型分类不是瞎猜？

**【答】**

三个机制：

1. **受限分类**：category 只能从 8 个枚举里选，不能自由发挥；
2. **强制 evidence**：每条必须给出 issue 原文片段，我可以人工抽查；
3. **兜底策略**：解析失败或模型异常时，整批标为 other，不混入错误结论。

### Q5：这个项目和你申请的 DevRel Content 有什么关系？

**【答】**

Content Intern 不是只写教程，而是要知道“写什么”。这个雷达解决的问题就是：在 432 条反馈里，哪些问题最高频、哪些最致命、哪些应该用文档解决。它把我的内容生产从“我觉得开发者需要”变成了“数据证明开发者需要”。
